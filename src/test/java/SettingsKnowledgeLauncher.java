/** Bootstrap so `java -cp` can start the settings/knowledge verify harness (same as SmokeLauncher). */
public class SettingsKnowledgeLauncher {
    public static void main(String[] args) {
        SettingsKnowledgeVerify.main(args);
    }
}
