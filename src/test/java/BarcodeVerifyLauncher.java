/** Bootstrap so `java -cp` can start the JavaFX verify harness (same as SmokeLauncher). */
public class BarcodeVerifyLauncher {
    public static void main(String[] args) {
        BarcodeFixVerify.main(args);
    }
}
