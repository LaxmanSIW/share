/** Bootstrap so `java -cp` can start the bulk-dialog verify harness (same as SmokeLauncher). */
public class BulkVerifyLauncher {
    public static void main(String[] args) {
        BulkDialogVerify.main(args);
    }
}
